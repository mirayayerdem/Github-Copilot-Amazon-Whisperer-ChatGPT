
def unique_digits(x):
    """ (int) -> bool

    Return True if and only if x has no repeated digits.

    >>> unique_digits(123)
    True
    >>> unique_digits(122)
    False
    """
    # Hint: use the % and // operators to extract digits.
    # Hint: use the // operator to remove the last digit.
    # Hint: use the % operator to extract the last digit.
    # Hint: use the in operator to check whether a digit is in a list.
    # Hint: use the append method to add a digit to a list.

    # The following line is a placeholder. Remove it.
    return False