
def check_if_last_char_is_a_letter(txt):
    """ (str) -> bool

    Return True if the last character in txt is a letter, and False
    otherwise.

    >>> check_if_last_char_is_a_letter('hello')
    True
    >>> check_if_last_char_is_a_letter('hello!')
    False
    >>> check_if_last_char_is_a_letter('hello.')
    False
    """
    # Hint: use the in operator to check whether a character is in a string.
    # Hint: use the len function to get the length of a string.
    # Hint: use the index operator to get a character from a string.
    # Hint: use the isalpha method to check whether a character is a letter.

    # The following line is a placeholder. Remove it.
    return False